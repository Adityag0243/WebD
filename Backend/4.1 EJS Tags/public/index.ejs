<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> 
        <%= title %>
    </h1>
    <p>
        Current secon is <%= seconds %> .
    </p>
    
        <% const sc = seconds %>
        <% if(sc % 2 == 0){ %> 
            <ul>
                <!-- <% const arr = items  %> -->
                <% for(let i = 0 ; i < items.length ; i++){ %> 
                    <li>
                        <%= items[i] %>
                    </li>
                <% } %>
            </ul> 
        <% }else{ %>
            <p>Nothing in the cart..</p>
        <% } %>
    
    <%- htmlContent %>
    <%- include("footer.ejs") %>
    
</body>
</html>